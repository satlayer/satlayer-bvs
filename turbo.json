{
  "$schema": "https://turbo.build/schema.json",
  "ui": "tui",
  "tasks": {
    "clean": {
      "cache": false
    },
    "generate:schema": {
      "inputs": ["src/**"],
      "outputs": ["schema/**"]
    },
    "build": {
      "inputs": ["tsconfig.json", "tsconfig.build.json", "src/**"],
      "outputs": ["dist/**"],
      "dependsOn": ["^build"]
    },
    "build:wasm": {
      "inputs": ["src/**"],
      "outputs": ["artifacts/**"],
      "dependsOn": ["@packages/docker-cosmwasm-optimizer#build"]
    },
    "test": {
      "inputs": ["tsconfig.json", "tsconfig.build.json", "src/**"],
      "dependsOn": ["^build"]
    },
    "test:e2e": {
      "inputs": ["tsconfig.json", "tsconfig.build.json", "src/**"],
      "dependsOn": ["^build"]
    },
    "eslint": {
      "cache": false,
      "outputLogs": "errors-only"
    }
  }
}
