{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "test": {
      "inputs": ["**.go"],
      "dependsOn": ["@modules/bvs-cw#generate:schema"]
    },
    "build": {
      "inputs": ["**.go"],
      "outputs": ["dist"],
      "dependsOn": ["@modules/bvs-cw#generate:schema"]
    }
  }
}
