{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "generate:schema": {
      "inputs": ["generate.mjs"],
      "outputs": ["types"],
      "dependsOn": ["^generate:schema"]
    },
    "build:wasm": {
      "dependsOn": ["^build:wasm"]
    }
  }
}
