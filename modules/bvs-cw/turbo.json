{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "generate:schema": {
      "inputs": ["generate.mjs"],
      "outputs": ["*.go"],
      "dependsOn": ["^generate:schema"]
    }
  }
}
