{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "test": {
      "inputs": ["**.go"],
      "dependsOn": ["@modules/cosmwasm-schema#generate:schema", "@modules/babylond#build:wasm"]
    }
  }
}
