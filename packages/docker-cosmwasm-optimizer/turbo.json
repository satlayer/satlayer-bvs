{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "docker:build": {
      "inputs": ["Dockerfile.optimizer", "Dockerfile.builder", "bin.js", "optimizer.sh"],
      "outputs": []
    },
    "build": {
      "dependsOn": ["docker:build"]
    }
  }
}
