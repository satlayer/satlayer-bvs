{
  "name": "@examples/squaring-contract",
  "private": false,
  "files": [
    "artifacts",
    "schema"
  ],
  "scripts": {
    "build": "docker run --pull=always --rm -v \"$(pwd)/../../../crates\":/crates -v \"$(pwd)\":/code --mount type=volume,source=\"$(basename \"$(pwd)\")_cache\",target=/target --mount type=volume,source=registry_cache,target=/usr/local/cargo/registry cosmwasm/optimizer:0.16.0",
    "generate:schema": "cargo run --bin schema --no-default-features",
    "test": "cargo test"
  }
}
