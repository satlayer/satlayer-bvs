{
  "name": "@satlayer/squaring",
  "private": true,
  "scripts": {
    "build": "go build -o ${PWD}/dist/squaring-task-cli ${PWD}/task/main.go && go build -o ${PWD}/dist/squaring-aggregator ${PWD}/aggregator/. && go build -o ${PWD}/dist/squaring-offchain ${PWD}/offchain/. && go build -o ${PWD}/dist/squaring-uploader ${PWD}/uploader/.",
    "test": "go test -v ./..."
  }
}
