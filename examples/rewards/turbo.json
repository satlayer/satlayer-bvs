{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "test": {
      "inputs": ["**.go"]
    },
    "build": {
      "inputs": ["**.go"],
      "outputs": ["dist"]
    }
  }
}
