{
  "name": "@satlayer/bvs-directory",
  "private": false,
  "scripts": {
    "build": "docker run --rm -v $(pwd)/..:/code --mount type=volume,source=satlayer_bvs_crates_cache,target=/target --mount type=volume,source=registry_cache,target=/usr/local/cargo/registry ghcr.io/satlayer/cosmwasm-optimizer:local ./$(basename $(pwd))",
    "generate:schema": "cargo run --bin schema",
    "test": "cargo test"
  },
  "dependencies": {
    "@workspace/docker-cosmwasm-optimizer": "workspace:*"
  }
}
